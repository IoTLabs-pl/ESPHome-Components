esphome:
  name: test

esp32:
  board: esp32dev
  framework:
    type: esp-idf

external_components:
  - source: ../components

wifi:
  ssid: TestSSID
  password: TestPass

uart:
  - id: heatpump_uart
    tx_pin: GPIO0
    rx_pin: GPIO1
    baud_rate: 9600
    data_bits: 8
    parity: EVEN
    stop_bits: 1

panasonic_aquarea:
  uart_id: heatpump_uart

sensor:
  - platform: panasonic_aquarea
    top: 1
  - platform: panasonic_aquarea
    top: 142

switch:
  - platform: panasonic_aquarea
    set: 1
  - platform: panasonic_aquarea
    set: 47

binary_sensor:
  - platform: panasonic_aquarea
    top: 26

text_sensor:
  - platform: panasonic_aquarea
    top: 20

select:
  - platform: panasonic_aquarea
    set: 39

number:
  - platform: panasonic_aquarea
    set: 46

button:
  - platform: panasonic_aquarea
    set: 48
  - platform: panasonic_aquarea
    set: "14:0"
